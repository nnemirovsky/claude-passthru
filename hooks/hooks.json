{
  "description": "passthru - regex-based permission rules for Claude Code",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/handlers/pre-tool-use.sh",
            "timeout": 2
          }
        ]
      }
    ]
  }
}
